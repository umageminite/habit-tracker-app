# Disable automatic script execution for security
# Protects against malicious install scripts in dependencies
ignore-scripts=true

# Use the modern registry (uses SHA-256, not SHA-1)
registry=https://registry.npmjs.org/

# Verify package integrity
strict-peer-dependencies=false
audit=true
